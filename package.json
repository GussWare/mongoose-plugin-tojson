{
  "name": "mongoose-plugin-tojson",
  "version": "0.0.1-dev",
  "description": "Plugin que convierte una colección en json, quita valores como _id y los converte en id",
  "main": "dist/MongoosePluginToJson.js",
  "scripts": {
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run tsc && node dist/MongoosePluginToJson.js"
  },
  "keywords": [
    "mongoose",
    "plugin",
    "ts",
    "tsc",
    "typescript"
  ],
  "author": "GussWare",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
